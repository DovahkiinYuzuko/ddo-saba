# DDO Saba 開発タスク（TODO）

## 1. 設計と調査フェーズ
- [x] ユズコのアイデアログの解析
- [x] 軽量ChatUIおよびCloudflare Tunnel連携方法の調査
- [x] 3カラムレイアウト、i18n、Lucide、動的モデル/ps機能の設計取り込み
- [x] DDO Saba仕様書.md の作成

## 2. 実装フェーズ（設計承認後に着手）
- [x] `docs\variables'n'functions` フォルダの作成（変数・関数仕様書管理用）
- [x] 各ソースファイルに対応する仕様ドキュメントの作成
- [x] .gitignore の設定（※事前にユズコの許可を得ること）
- [x] Nginxの設定ファイルの作成
  - [x] `nginx/conf/nginx.conf` （プロキシ、同時接続制限、静的ファイル配信設定）
  - [x] `nginx/conf/broadcast.js` （njsによるリアルタイム中継スクリプト）
- [x] Vite + React による3カラムChatUIの作成
  - [x] `web-ui/` プロジェクトの作成（Vite + React + TypeScript + Vanilla CSS）
  - [x] UIレイアウトの実装（3カラム：左タブ管理、中チャットログ/思考表示、右パラメータ設定（min_pなどのスライダー含む））
  - [x] 多言語（i18n）辞書オブジェクトの実装（基本言語：英語）
  - [x] カセット機能の実装（JSONファイルのエクスポートとインポート）
  - [x] 思考モード制御（`/api/chat` への `think` パラメータ送信、UI上の `<think>` アコーディオン表示）
  - [x] モデル動的取得（`/api/tags`）と現在ロード中のモデル情報表示（`/api/ps`）の統合
  - [x] 設定モーダルの実装（Connection URL、トークン設定、共有/お一人様トグル）
  - [x] ビルド成果物の `nginx/html/` への出力設定（Vite config調整）と同梱の確認
- [x] 起動用スクリプトの作成
  - [x] `start_server.bat` （起動バッチ）
  - [x] `bin/start_tunnel.ps1` （PowerShellによるCloudflare Tunnel起動＆URL自動抽出・起動スクリプト）
  - [x] `start_server.sh` （Linux用起動シェルスクリプト）

## 3. テスト・検証フェーズ
- [x] ローカル環境での起動テスト（Windows/Linux）
- [/] Cloudflare Tunnel経由での外部接続テスト
- [ ] 複数クライアントでのリアルタイム中継の動作検証
- [x] カセット（JSONファイル）による履歴のエクスポート・インポート機能の検証
- [ ] 同時接続制限（早い者勝ちロック）の検証
- [x] /api/tags および /api/ps の動的反映の検証

## 4. UI改善・メトリクス追加フェーズ（追加要望）
- [x] チャット画面のスクロール挙動の修正およびスクロールバーの視認性向上
- [x] AIアシスタントのアイコンと表示名のモデル名化（`activeModel` との動的連動）
- [x] 思考時間（CoT）、総出力時間、生成速度（tok/s）、トークン量のメトリクス表示
- [x] コンテキスト限界パーセントの右サイドバー表示
- [x] Enter/Shift+Enterによる送信キー設定のカスタムトグル追加

## 5. 追加要望機能フェーズ（やりたいこと.md）
- [x] チャット内の簡易Markdownレンダリングの実装（太字、インラインコード、コードブロック、改行の対応）
- [x] 生成待機中・読み込み中のローディングスピナー（グルグルアイコン）表示
- [x] 各メッセージ（プロンプトとAI出力）のコピー機能（ホバー時のコピーボタン）
- [x] 稼働中モデルの能動的アンロード機能（keep_alive: 0 によるVRAM解放ボタン）
- [x] パラメータ類の拡充（コンテキストサイズ num_ctx の変更スライダー追加）
- [x] Top_PとMin_Pの変更ステップを0.01単位へ調整

## 6. UI機能強化＆Ollama連携最適化フェーズ（2026-06-20）
- [x] 最大出力トークン数（`num_predict`）のトグル化と最大値の拡張（最大16384）
- [x] モデル選択ドロップダウンでのファイルサイズ表示（MB、GB、TB）
- [x] 設定プリセットのエクスポート・インポート機能（プリセット名編集保存含む）
- [x] 日本語ローカライズ修正（`num_ctx` の表示を「コンテキスト制限 (num_ctx)」に修正）
- [x] モデル選択時の自動プリロード処理の実装（`/api/generate` を使用）
- [x] ロード中のモデル選択ドロップダウンのロック（`disabled`）処理
- [x] 初期状態としての「モデル未選択」状態の追加
- [x] 4分ごとのキープアライブ（リフレッシュ ping）の実装
- [x] Nginx 設定ファイル（`nginx.conf` と `nginx_no_njs.conf`）への `/api/generate` プロキシ設定の追加と再起動
- [x] 思考プロセス（DeepSeek-R1 `thinking`）のストリーミング描画とスピナー演出
- [x] 思考プロセスのアコーディオン手動トグル展開制御の実装
  - [x] 展開トグル（矢印など）がクリックされた時のみ思考内容を表示し、閉じている時は「Thinking...」ぐるぐるスピナーのみ表示するようにオンオフ制御する
- [x] ドロップダウン周辺のレイアウト崩れ修正
  - [x] `.model-selector-wrap` からインラインの `width: '100%'` や縦並びを除去する
  - [x] ロード中のテキスト（`Loading Model...`）やエラー（`modelLoadError`）を **絶対配置（`position: 'absolute'`）** に変更し、ヘッダーの高さが押し広げられてセレクトボックスが上に押し潰されるのを防ぐ
- [x] VRAMアンロードボタンのコンパクト化と配置変更
  - [x] サイズを `32px` に縮め、背景と枠線を透明（ボーダーレス）に変更する
  - [x] モデル選択セレクトボックスのすぐ右隣に `gap: 8px` で綺麗に高さを揃えて配置（Ollama psエリアから移動）する
- [x] プリセット名入力欄のデザイン統一
  - [x] プリセット名入力インプットのインラインスタイルを剥がし、`index.css` に定義する標準の入力欄スタイル（`.preset-name-input`）を適用する
  - [x] 他の項目と同様に `.input-group` で囲い、上に「プリセット名」のラベルを配置してデザインを調和させる
- [x] プリセットアクションボタンの統一
  - [x] エクスポート・インポートボタンの英語テキストを `Export` と `Import` に短縮する
  - [x] インラインスタイルを撤去し、標準の `.btn-secondary` のパディングとフォントサイズをそのまま適用して他のセカンダリボタンと統一する
- [x] UI全体のレイアウト崩れ・表示異常の調査と修正
  - [x] チャット画面、左右サイドバー、ヘッダー等の全体のレイアウトをチェックし、崩れや使いにくい箇所がないか総合的に確認して修正する
